# BugFreeCode Editor - Todo

## Steps
- [ ] Add real login page logic (email/password) using existing localStorage user format.
- [ ] Gate access to `index.html` so direct opening without login redirects to `login.html`.
- [x] Remove/disable DEMO auto-login in `index.html`.
- [ ] Verify signup → login → index redirect flow works end-to-end.


